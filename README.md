# Умный электротранспорт NUANU — Модуль реактивной озвучки

## 🧠 Описание
Это прототип системы **реактивной голосовой озвучки** для проекта «Умный электротранспорт-гид NUANU».  
Система использует **OpenAI GPT‑4o‑mini** для генерации коротких юмористических комментариев и  
**ElevenLabs Streaming API** для синтеза речи в реальном времени.  
Голос транспортного средства реагирует на окружающие объекты и комментирует происходящее, создавая эффект «живого» персонажа.

---

## 🚀 Возможности
- 🔊 Реактивная голосовая реакция на события (например: «камера, две собаки слева»).
- 🧩 Потоковый синтез речи через ElevenLabs (звуки проигрываются без пауз).
- 🌍 Автоматическое определение языка (русский / английский).
- 🤖 Генерация реплик с юмором и контекстом через GPT‑4o‑mini.
- 🎧 Непрерывное воспроизведение без обрывов и задержек.

---

## ⚙️ Установка

### Требования
- Python ≥ 3.11  
- Windows или Linux (Jetson Orin протестирован)

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Настройка ключей
Создайте файл `.env` в корне проекта и укажите ключи API:
```bash
OPENAI_API_KEY=ваш_ключ_OpenAI
ELEVENLABS_API_KEY=ваш_ключ_ElevenLabs
```
Файл `.env` добавлен в `.gitignore`, поэтому он не попадёт в репозиторий.

---

## ▶️ Использование
Запуск демо:
```bash
python tts_test_1.py
```

Введите фразу, например:
```
камера, две собаки слева
```
Система:
1. Отправляет фразу в GPT‑4o‑mini → создаёт реплику.
2. Передаёт её в ElevenLabs Streaming API.
3. Мгновенно начинает озвучку в реальном времени.

Завершить работу можно командой:
```
exit
```

---

## 🧩 Настройки голосов
В коде можно изменить голос и модель:
```python
VOICE_ID = "y2Y5MeVPm6ZQXK64WUui"  # ваш голос ElevenLabs
MODEL_ID = "eleven_flash_v2_5"
```
Поддерживаются голоса с мультиязычной озвучкой.

---

## ⚡ Известные особенности
- Возможны незначительные предупреждения `mpg123` при завершении потока — это нормально.
- Скорость отклика зависит от интернет‑соединения.
- ElevenLabs может обрезать последние 0.1–0.2 секунды речи — при необходимости можно добавить небольшую паузу перед закрытием потока.

---

## 📄 Лицензия
Модуль является частью проекта **«Умный электротранспорт‑гид NUANU»**.  
© 2025 Денис Новиков — Все права защищены.
